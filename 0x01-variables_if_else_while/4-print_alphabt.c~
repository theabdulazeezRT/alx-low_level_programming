#include <stdio.h>
/**
* main - print the alphabet in lowercase, except for e and q
*
*Return: Always 0 (success)
*/
int main(void)
{
char b;
for (b = 'a'; b <=z; b++)
{
if (b != 'e' && b != 'q')
putchar(b);
}
putchar('\n');

return (0);
}